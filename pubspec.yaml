name: mobile_sched
description: A premium animated attendance MVP.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.2.1
  flutter_animate: ^4.5.0
  provider: ^6.0.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  flutter_local_notifications: ^17.1.0
  http: ^1.1.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true